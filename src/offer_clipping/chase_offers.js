javascript: a = () => { window.history.back(); setTimeout(c, Math.random() * 1000 + 500); }; c = () => { btns = [...document.querySelectorAll('.r9jbijc')].filter(b => { const parent = b.parentNode; const siblings = [...parent.children]; return siblings.some(sibling => sibling !== b && sibling.querySelector('mds-icon[type="ico_add_circle"]')); }); b = btns.pop(); if (!b) return console.log('added all!'); b.childNodes[0].click(); setTimeout(a, Math.random() * 1000 + 200); }; c();
